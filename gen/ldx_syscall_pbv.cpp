/* AUTO-GENERATED by gen_syscall_pbv.py — do not edit */
#include "ldx_syscall_pbv.h"

extern "C" {
#include "ldx.h"
}

#include <cstdio>
#include <cstring>
#include <dlfcn.h>

/* --- Pipe instances --- */

static ldx_sc_read_pipe_t *sc_pipe_read = nullptr;
static ldx_sc_write_pipe_t *sc_pipe_write = nullptr;
static ldx_sc_pread_pipe_t *sc_pipe_pread = nullptr;
static ldx_sc_pwrite_pipe_t *sc_pipe_pwrite = nullptr;
static ldx_sc_close_pipe_t *sc_pipe_close = nullptr;
static ldx_sc_lseek_pipe_t *sc_pipe_lseek = nullptr;
static ldx_sc_dup_pipe_t *sc_pipe_dup = nullptr;
static ldx_sc_dup2_pipe_t *sc_pipe_dup2 = nullptr;
static ldx_sc_pipe_pipe_t *sc_pipe_pipe = nullptr;
static ldx_sc_stat_pipe_t *sc_pipe_stat = nullptr;
static ldx_sc_fstat_pipe_t *sc_pipe_fstat = nullptr;
static ldx_sc_lstat_pipe_t *sc_pipe_lstat = nullptr;
static ldx_sc_access_pipe_t *sc_pipe_access = nullptr;
static ldx_sc_unlink_pipe_t *sc_pipe_unlink = nullptr;
static ldx_sc_rmdir_pipe_t *sc_pipe_rmdir = nullptr;
static ldx_sc_mkdir_pipe_t *sc_pipe_mkdir = nullptr;
static ldx_sc_chmod_pipe_t *sc_pipe_chmod = nullptr;
static ldx_sc_fchmod_pipe_t *sc_pipe_fchmod = nullptr;
static ldx_sc_chown_pipe_t *sc_pipe_chown = nullptr;
static ldx_sc_fchown_pipe_t *sc_pipe_fchown = nullptr;
static ldx_sc_link_pipe_t *sc_pipe_link = nullptr;
static ldx_sc_symlink_pipe_t *sc_pipe_symlink = nullptr;
static ldx_sc_readlink_pipe_t *sc_pipe_readlink = nullptr;
static ldx_sc_chdir_pipe_t *sc_pipe_chdir = nullptr;
static ldx_sc_fchdir_pipe_t *sc_pipe_fchdir = nullptr;
static ldx_sc_umask_pipe_t *sc_pipe_umask = nullptr;
static ldx_sc_rename_pipe_t *sc_pipe_rename = nullptr;
static ldx_sc_truncate_pipe_t *sc_pipe_truncate = nullptr;
static ldx_sc_ftruncate_pipe_t *sc_pipe_ftruncate = nullptr;
static ldx_sc_socket_pipe_t *sc_pipe_socket = nullptr;
static ldx_sc_bind_pipe_t *sc_pipe_bind = nullptr;
static ldx_sc_connect_pipe_t *sc_pipe_connect = nullptr;
static ldx_sc_listen_pipe_t *sc_pipe_listen = nullptr;
static ldx_sc_shutdown_pipe_t *sc_pipe_shutdown = nullptr;
static ldx_sc_send_pipe_t *sc_pipe_send = nullptr;
static ldx_sc_recv_pipe_t *sc_pipe_recv = nullptr;
static ldx_sc_setsockopt_pipe_t *sc_pipe_setsockopt = nullptr;
static ldx_sc_mmap_pipe_t *sc_pipe_mmap = nullptr;
static ldx_sc_munmap_pipe_t *sc_pipe_munmap = nullptr;
static ldx_sc_mprotect_pipe_t *sc_pipe_mprotect = nullptr;
static ldx_sc_getpid_pipe_t *sc_pipe_getpid = nullptr;
static ldx_sc_getppid_pipe_t *sc_pipe_getppid = nullptr;
static ldx_sc_getuid_pipe_t *sc_pipe_getuid = nullptr;
static ldx_sc_geteuid_pipe_t *sc_pipe_geteuid = nullptr;
static ldx_sc_getgid_pipe_t *sc_pipe_getgid = nullptr;
static ldx_sc_getegid_pipe_t *sc_pipe_getegid = nullptr;
static ldx_sc_setuid_pipe_t *sc_pipe_setuid = nullptr;
static ldx_sc_setgid_pipe_t *sc_pipe_setgid = nullptr;
static ldx_sc_kill_pipe_t *sc_pipe_kill = nullptr;
static ldx_sc_epoll_create1_pipe_t *sc_pipe_epoll_create1 = nullptr;
static ldx_sc_epoll_ctl_pipe_t *sc_pipe_epoll_ctl = nullptr;
static ldx_sc_gethostname_pipe_t *sc_pipe_gethostname = nullptr;
static ldx_sc_sethostname_pipe_t *sc_pipe_sethostname = nullptr;

/* --- Wrapper functions --- */

static ssize_t wrap_read(int fd, void * buf, size_t n)
{
    ldx_sc_read_args args = {fd, buf, n};
    return sc_pipe_read->call(args);
}

static ssize_t wrap_write(int fd, const void * buf, size_t n)
{
    ldx_sc_write_args args = {fd, buf, n};
    return sc_pipe_write->call(args);
}

static ssize_t wrap_pread(int fd, void * buf, size_t n, off_t offset)
{
    ldx_sc_pread_args args = {fd, buf, n, offset};
    return sc_pipe_pread->call(args);
}

static ssize_t wrap_pwrite(int fd, const void * buf, size_t n, off_t offset)
{
    ldx_sc_pwrite_args args = {fd, buf, n, offset};
    return sc_pipe_pwrite->call(args);
}

static int wrap_close(int fd)
{
    ldx_sc_close_args args = {fd};
    return sc_pipe_close->call(args);
}

static off_t wrap_lseek(int fd, off_t offset, int whence)
{
    ldx_sc_lseek_args args = {fd, offset, whence};
    return sc_pipe_lseek->call(args);
}

static int wrap_dup(int fd)
{
    ldx_sc_dup_args args = {fd};
    return sc_pipe_dup->call(args);
}

static int wrap_dup2(int fd, int fd2)
{
    ldx_sc_dup2_args args = {fd, fd2};
    return sc_pipe_dup2->call(args);
}

static int wrap_pipe(int * pipedes)
{
    ldx_sc_pipe_args args = {pipedes};
    return sc_pipe_pipe->call(args);
}

static int wrap_stat(const char * path, struct stat * buf)
{
    ldx_sc_stat_args args = {path, buf};
    return sc_pipe_stat->call(args);
}

static int wrap_fstat(int fd, struct stat * buf)
{
    ldx_sc_fstat_args args = {fd, buf};
    return sc_pipe_fstat->call(args);
}

static int wrap_lstat(const char * path, struct stat * buf)
{
    ldx_sc_lstat_args args = {path, buf};
    return sc_pipe_lstat->call(args);
}

static int wrap_access(const char * path, int mode)
{
    ldx_sc_access_args args = {path, mode};
    return sc_pipe_access->call(args);
}

static int wrap_unlink(const char * path)
{
    ldx_sc_unlink_args args = {path};
    return sc_pipe_unlink->call(args);
}

static int wrap_rmdir(const char * path)
{
    ldx_sc_rmdir_args args = {path};
    return sc_pipe_rmdir->call(args);
}

static int wrap_mkdir(const char * path, mode_t mode)
{
    ldx_sc_mkdir_args args = {path, mode};
    return sc_pipe_mkdir->call(args);
}

static int wrap_chmod(const char * path, mode_t mode)
{
    ldx_sc_chmod_args args = {path, mode};
    return sc_pipe_chmod->call(args);
}

static int wrap_fchmod(int fd, mode_t mode)
{
    ldx_sc_fchmod_args args = {fd, mode};
    return sc_pipe_fchmod->call(args);
}

static int wrap_chown(const char * path, uid_t owner, gid_t group)
{
    ldx_sc_chown_args args = {path, owner, group};
    return sc_pipe_chown->call(args);
}

static int wrap_fchown(int fd, uid_t owner, gid_t group)
{
    ldx_sc_fchown_args args = {fd, owner, group};
    return sc_pipe_fchown->call(args);
}

static int wrap_link(const char * from_path, const char * to_path)
{
    ldx_sc_link_args args = {from_path, to_path};
    return sc_pipe_link->call(args);
}

static int wrap_symlink(const char * from_path, const char * to_path)
{
    ldx_sc_symlink_args args = {from_path, to_path};
    return sc_pipe_symlink->call(args);
}

static ssize_t wrap_readlink(const char * path, char * buf, size_t len)
{
    ldx_sc_readlink_args args = {path, buf, len};
    return sc_pipe_readlink->call(args);
}

static int wrap_chdir(const char * path)
{
    ldx_sc_chdir_args args = {path};
    return sc_pipe_chdir->call(args);
}

static int wrap_fchdir(int fd)
{
    ldx_sc_fchdir_args args = {fd};
    return sc_pipe_fchdir->call(args);
}

static mode_t wrap_umask(mode_t mask)
{
    ldx_sc_umask_args args = {mask};
    return sc_pipe_umask->call(args);
}

static int wrap_rename(const char * old_path, const char * new_path)
{
    ldx_sc_rename_args args = {old_path, new_path};
    return sc_pipe_rename->call(args);
}

static int wrap_truncate(const char * path, off_t length)
{
    ldx_sc_truncate_args args = {path, length};
    return sc_pipe_truncate->call(args);
}

static int wrap_ftruncate(int fd, off_t length)
{
    ldx_sc_ftruncate_args args = {fd, length};
    return sc_pipe_ftruncate->call(args);
}

static int wrap_socket(int domain, int type, int protocol)
{
    ldx_sc_socket_args args = {domain, type, protocol};
    return sc_pipe_socket->call(args);
}

static int wrap_bind(int fd, const struct sockaddr * addr, socklen_t len)
{
    ldx_sc_bind_args args = {fd, addr, len};
    return sc_pipe_bind->call(args);
}

static int wrap_connect(int fd, const struct sockaddr * addr, socklen_t len)
{
    ldx_sc_connect_args args = {fd, addr, len};
    return sc_pipe_connect->call(args);
}

static int wrap_listen(int fd, int backlog)
{
    ldx_sc_listen_args args = {fd, backlog};
    return sc_pipe_listen->call(args);
}

static int wrap_shutdown(int fd, int how)
{
    ldx_sc_shutdown_args args = {fd, how};
    return sc_pipe_shutdown->call(args);
}

static ssize_t wrap_send(int fd, const void * buf, size_t n, int flags)
{
    ldx_sc_send_args args = {fd, buf, n, flags};
    return sc_pipe_send->call(args);
}

static ssize_t wrap_recv(int fd, void * buf, size_t n, int flags)
{
    ldx_sc_recv_args args = {fd, buf, n, flags};
    return sc_pipe_recv->call(args);
}

static int wrap_setsockopt(int fd, int level, int optname, const void * optval, socklen_t optlen)
{
    ldx_sc_setsockopt_args args = {fd, level, optname, optval, optlen};
    return sc_pipe_setsockopt->call(args);
}

static void * wrap_mmap(void * addr, size_t len, int prot, int flags, int fd, off_t offset)
{
    ldx_sc_mmap_args args = {addr, len, prot, flags, fd, offset};
    return sc_pipe_mmap->call(args);
}

static int wrap_munmap(void * addr, size_t len)
{
    ldx_sc_munmap_args args = {addr, len};
    return sc_pipe_munmap->call(args);
}

static int wrap_mprotect(void * addr, size_t len, int prot)
{
    ldx_sc_mprotect_args args = {addr, len, prot};
    return sc_pipe_mprotect->call(args);
}

static pid_t wrap_getpid(void)
{
    ldx_sc_getpid_args args = {};
    return sc_pipe_getpid->call(args);
}

static pid_t wrap_getppid(void)
{
    ldx_sc_getppid_args args = {};
    return sc_pipe_getppid->call(args);
}

static uid_t wrap_getuid(void)
{
    ldx_sc_getuid_args args = {};
    return sc_pipe_getuid->call(args);
}

static uid_t wrap_geteuid(void)
{
    ldx_sc_geteuid_args args = {};
    return sc_pipe_geteuid->call(args);
}

static gid_t wrap_getgid(void)
{
    ldx_sc_getgid_args args = {};
    return sc_pipe_getgid->call(args);
}

static gid_t wrap_getegid(void)
{
    ldx_sc_getegid_args args = {};
    return sc_pipe_getegid->call(args);
}

static int wrap_setuid(uid_t uid)
{
    ldx_sc_setuid_args args = {uid};
    return sc_pipe_setuid->call(args);
}

static int wrap_setgid(gid_t gid)
{
    ldx_sc_setgid_args args = {gid};
    return sc_pipe_setgid->call(args);
}

static int wrap_kill(pid_t pid, int sig)
{
    ldx_sc_kill_args args = {pid, sig};
    return sc_pipe_kill->call(args);
}

static int wrap_epoll_create1(int flags)
{
    ldx_sc_epoll_create1_args args = {flags};
    return sc_pipe_epoll_create1->call(args);
}

static int wrap_epoll_ctl(int epfd, int op, int fd, struct epoll_event * event)
{
    ldx_sc_epoll_ctl_args args = {epfd, op, fd, event};
    return sc_pipe_epoll_ctl->call(args);
}

static int wrap_gethostname(char * name, size_t len)
{
    ldx_sc_gethostname_args args = {name, len};
    return sc_pipe_gethostname->call(args);
}

static int wrap_sethostname(const char * name, size_t len)
{
    ldx_sc_sethostname_args args = {name, len};
    return sc_pipe_sethostname->call(args);
}

/* --- Init: save originals, create pipes, patch GOT --- */

static int installed_count = 0;

extern "C" int ldx_syscall_pbv_init(void)
{
    ldx_init();
    void *orig;

    /* read */
    orig = dlsym(RTLD_NEXT, "read");
    if (orig) {
        sc_pipe_read = new ldx_sc_read_pipe_t(orig);
        dlreplace("read", (void *)wrap_read);
        installed_count++;
    }

    /* write */
    orig = dlsym(RTLD_NEXT, "write");
    if (orig) {
        sc_pipe_write = new ldx_sc_write_pipe_t(orig);
        dlreplace("write", (void *)wrap_write);
        installed_count++;
    }

    /* pread */
    orig = dlsym(RTLD_NEXT, "pread");
    if (orig) {
        sc_pipe_pread = new ldx_sc_pread_pipe_t(orig);
        dlreplace("pread", (void *)wrap_pread);
        installed_count++;
    }

    /* pwrite */
    orig = dlsym(RTLD_NEXT, "pwrite");
    if (orig) {
        sc_pipe_pwrite = new ldx_sc_pwrite_pipe_t(orig);
        dlreplace("pwrite", (void *)wrap_pwrite);
        installed_count++;
    }

    /* close */
    orig = dlsym(RTLD_NEXT, "close");
    if (orig) {
        sc_pipe_close = new ldx_sc_close_pipe_t(orig);
        dlreplace("close", (void *)wrap_close);
        installed_count++;
    }

    /* lseek */
    orig = dlsym(RTLD_NEXT, "lseek");
    if (orig) {
        sc_pipe_lseek = new ldx_sc_lseek_pipe_t(orig);
        dlreplace("lseek", (void *)wrap_lseek);
        installed_count++;
    }

    /* dup */
    orig = dlsym(RTLD_NEXT, "dup");
    if (orig) {
        sc_pipe_dup = new ldx_sc_dup_pipe_t(orig);
        dlreplace("dup", (void *)wrap_dup);
        installed_count++;
    }

    /* dup2 */
    orig = dlsym(RTLD_NEXT, "dup2");
    if (orig) {
        sc_pipe_dup2 = new ldx_sc_dup2_pipe_t(orig);
        dlreplace("dup2", (void *)wrap_dup2);
        installed_count++;
    }

    /* pipe */
    orig = dlsym(RTLD_NEXT, "pipe");
    if (orig) {
        sc_pipe_pipe = new ldx_sc_pipe_pipe_t(orig);
        dlreplace("pipe", (void *)wrap_pipe);
        installed_count++;
    }

    /* stat */
    orig = dlsym(RTLD_NEXT, "stat");
    if (orig) {
        sc_pipe_stat = new ldx_sc_stat_pipe_t(orig);
        dlreplace("stat", (void *)wrap_stat);
        installed_count++;
    }

    /* fstat */
    orig = dlsym(RTLD_NEXT, "fstat");
    if (orig) {
        sc_pipe_fstat = new ldx_sc_fstat_pipe_t(orig);
        dlreplace("fstat", (void *)wrap_fstat);
        installed_count++;
    }

    /* lstat */
    orig = dlsym(RTLD_NEXT, "lstat");
    if (orig) {
        sc_pipe_lstat = new ldx_sc_lstat_pipe_t(orig);
        dlreplace("lstat", (void *)wrap_lstat);
        installed_count++;
    }

    /* access */
    orig = dlsym(RTLD_NEXT, "access");
    if (orig) {
        sc_pipe_access = new ldx_sc_access_pipe_t(orig);
        dlreplace("access", (void *)wrap_access);
        installed_count++;
    }

    /* unlink */
    orig = dlsym(RTLD_NEXT, "unlink");
    if (orig) {
        sc_pipe_unlink = new ldx_sc_unlink_pipe_t(orig);
        dlreplace("unlink", (void *)wrap_unlink);
        installed_count++;
    }

    /* rmdir */
    orig = dlsym(RTLD_NEXT, "rmdir");
    if (orig) {
        sc_pipe_rmdir = new ldx_sc_rmdir_pipe_t(orig);
        dlreplace("rmdir", (void *)wrap_rmdir);
        installed_count++;
    }

    /* mkdir */
    orig = dlsym(RTLD_NEXT, "mkdir");
    if (orig) {
        sc_pipe_mkdir = new ldx_sc_mkdir_pipe_t(orig);
        dlreplace("mkdir", (void *)wrap_mkdir);
        installed_count++;
    }

    /* chmod */
    orig = dlsym(RTLD_NEXT, "chmod");
    if (orig) {
        sc_pipe_chmod = new ldx_sc_chmod_pipe_t(orig);
        dlreplace("chmod", (void *)wrap_chmod);
        installed_count++;
    }

    /* fchmod */
    orig = dlsym(RTLD_NEXT, "fchmod");
    if (orig) {
        sc_pipe_fchmod = new ldx_sc_fchmod_pipe_t(orig);
        dlreplace("fchmod", (void *)wrap_fchmod);
        installed_count++;
    }

    /* chown */
    orig = dlsym(RTLD_NEXT, "chown");
    if (orig) {
        sc_pipe_chown = new ldx_sc_chown_pipe_t(orig);
        dlreplace("chown", (void *)wrap_chown);
        installed_count++;
    }

    /* fchown */
    orig = dlsym(RTLD_NEXT, "fchown");
    if (orig) {
        sc_pipe_fchown = new ldx_sc_fchown_pipe_t(orig);
        dlreplace("fchown", (void *)wrap_fchown);
        installed_count++;
    }

    /* link */
    orig = dlsym(RTLD_NEXT, "link");
    if (orig) {
        sc_pipe_link = new ldx_sc_link_pipe_t(orig);
        dlreplace("link", (void *)wrap_link);
        installed_count++;
    }

    /* symlink */
    orig = dlsym(RTLD_NEXT, "symlink");
    if (orig) {
        sc_pipe_symlink = new ldx_sc_symlink_pipe_t(orig);
        dlreplace("symlink", (void *)wrap_symlink);
        installed_count++;
    }

    /* readlink */
    orig = dlsym(RTLD_NEXT, "readlink");
    if (orig) {
        sc_pipe_readlink = new ldx_sc_readlink_pipe_t(orig);
        dlreplace("readlink", (void *)wrap_readlink);
        installed_count++;
    }

    /* chdir */
    orig = dlsym(RTLD_NEXT, "chdir");
    if (orig) {
        sc_pipe_chdir = new ldx_sc_chdir_pipe_t(orig);
        dlreplace("chdir", (void *)wrap_chdir);
        installed_count++;
    }

    /* fchdir */
    orig = dlsym(RTLD_NEXT, "fchdir");
    if (orig) {
        sc_pipe_fchdir = new ldx_sc_fchdir_pipe_t(orig);
        dlreplace("fchdir", (void *)wrap_fchdir);
        installed_count++;
    }

    /* umask */
    orig = dlsym(RTLD_NEXT, "umask");
    if (orig) {
        sc_pipe_umask = new ldx_sc_umask_pipe_t(orig);
        dlreplace("umask", (void *)wrap_umask);
        installed_count++;
    }

    /* rename */
    orig = dlsym(RTLD_NEXT, "rename");
    if (orig) {
        sc_pipe_rename = new ldx_sc_rename_pipe_t(orig);
        dlreplace("rename", (void *)wrap_rename);
        installed_count++;
    }

    /* truncate */
    orig = dlsym(RTLD_NEXT, "truncate");
    if (orig) {
        sc_pipe_truncate = new ldx_sc_truncate_pipe_t(orig);
        dlreplace("truncate", (void *)wrap_truncate);
        installed_count++;
    }

    /* ftruncate */
    orig = dlsym(RTLD_NEXT, "ftruncate");
    if (orig) {
        sc_pipe_ftruncate = new ldx_sc_ftruncate_pipe_t(orig);
        dlreplace("ftruncate", (void *)wrap_ftruncate);
        installed_count++;
    }

    /* socket */
    orig = dlsym(RTLD_NEXT, "socket");
    if (orig) {
        sc_pipe_socket = new ldx_sc_socket_pipe_t(orig);
        dlreplace("socket", (void *)wrap_socket);
        installed_count++;
    }

    /* bind */
    orig = dlsym(RTLD_NEXT, "bind");
    if (orig) {
        sc_pipe_bind = new ldx_sc_bind_pipe_t(orig);
        dlreplace("bind", (void *)wrap_bind);
        installed_count++;
    }

    /* connect */
    orig = dlsym(RTLD_NEXT, "connect");
    if (orig) {
        sc_pipe_connect = new ldx_sc_connect_pipe_t(orig);
        dlreplace("connect", (void *)wrap_connect);
        installed_count++;
    }

    /* listen */
    orig = dlsym(RTLD_NEXT, "listen");
    if (orig) {
        sc_pipe_listen = new ldx_sc_listen_pipe_t(orig);
        dlreplace("listen", (void *)wrap_listen);
        installed_count++;
    }

    /* shutdown */
    orig = dlsym(RTLD_NEXT, "shutdown");
    if (orig) {
        sc_pipe_shutdown = new ldx_sc_shutdown_pipe_t(orig);
        dlreplace("shutdown", (void *)wrap_shutdown);
        installed_count++;
    }

    /* send */
    orig = dlsym(RTLD_NEXT, "send");
    if (orig) {
        sc_pipe_send = new ldx_sc_send_pipe_t(orig);
        dlreplace("send", (void *)wrap_send);
        installed_count++;
    }

    /* recv */
    orig = dlsym(RTLD_NEXT, "recv");
    if (orig) {
        sc_pipe_recv = new ldx_sc_recv_pipe_t(orig);
        dlreplace("recv", (void *)wrap_recv);
        installed_count++;
    }

    /* setsockopt */
    orig = dlsym(RTLD_NEXT, "setsockopt");
    if (orig) {
        sc_pipe_setsockopt = new ldx_sc_setsockopt_pipe_t(orig);
        dlreplace("setsockopt", (void *)wrap_setsockopt);
        installed_count++;
    }

    /* mmap */
    orig = dlsym(RTLD_NEXT, "mmap");
    if (orig) {
        sc_pipe_mmap = new ldx_sc_mmap_pipe_t(orig);
        dlreplace("mmap", (void *)wrap_mmap);
        installed_count++;
    }

    /* munmap */
    orig = dlsym(RTLD_NEXT, "munmap");
    if (orig) {
        sc_pipe_munmap = new ldx_sc_munmap_pipe_t(orig);
        dlreplace("munmap", (void *)wrap_munmap);
        installed_count++;
    }

    /* mprotect */
    orig = dlsym(RTLD_NEXT, "mprotect");
    if (orig) {
        sc_pipe_mprotect = new ldx_sc_mprotect_pipe_t(orig);
        dlreplace("mprotect", (void *)wrap_mprotect);
        installed_count++;
    }

    /* getpid */
    orig = dlsym(RTLD_NEXT, "getpid");
    if (orig) {
        sc_pipe_getpid = new ldx_sc_getpid_pipe_t(orig);
        dlreplace("getpid", (void *)wrap_getpid);
        installed_count++;
    }

    /* getppid */
    orig = dlsym(RTLD_NEXT, "getppid");
    if (orig) {
        sc_pipe_getppid = new ldx_sc_getppid_pipe_t(orig);
        dlreplace("getppid", (void *)wrap_getppid);
        installed_count++;
    }

    /* getuid */
    orig = dlsym(RTLD_NEXT, "getuid");
    if (orig) {
        sc_pipe_getuid = new ldx_sc_getuid_pipe_t(orig);
        dlreplace("getuid", (void *)wrap_getuid);
        installed_count++;
    }

    /* geteuid */
    orig = dlsym(RTLD_NEXT, "geteuid");
    if (orig) {
        sc_pipe_geteuid = new ldx_sc_geteuid_pipe_t(orig);
        dlreplace("geteuid", (void *)wrap_geteuid);
        installed_count++;
    }

    /* getgid */
    orig = dlsym(RTLD_NEXT, "getgid");
    if (orig) {
        sc_pipe_getgid = new ldx_sc_getgid_pipe_t(orig);
        dlreplace("getgid", (void *)wrap_getgid);
        installed_count++;
    }

    /* getegid */
    orig = dlsym(RTLD_NEXT, "getegid");
    if (orig) {
        sc_pipe_getegid = new ldx_sc_getegid_pipe_t(orig);
        dlreplace("getegid", (void *)wrap_getegid);
        installed_count++;
    }

    /* setuid */
    orig = dlsym(RTLD_NEXT, "setuid");
    if (orig) {
        sc_pipe_setuid = new ldx_sc_setuid_pipe_t(orig);
        dlreplace("setuid", (void *)wrap_setuid);
        installed_count++;
    }

    /* setgid */
    orig = dlsym(RTLD_NEXT, "setgid");
    if (orig) {
        sc_pipe_setgid = new ldx_sc_setgid_pipe_t(orig);
        dlreplace("setgid", (void *)wrap_setgid);
        installed_count++;
    }

    /* kill */
    orig = dlsym(RTLD_NEXT, "kill");
    if (orig) {
        sc_pipe_kill = new ldx_sc_kill_pipe_t(orig);
        dlreplace("kill", (void *)wrap_kill);
        installed_count++;
    }

    /* epoll_create1 */
    orig = dlsym(RTLD_NEXT, "epoll_create1");
    if (orig) {
        sc_pipe_epoll_create1 = new ldx_sc_epoll_create1_pipe_t(orig);
        dlreplace("epoll_create1", (void *)wrap_epoll_create1);
        installed_count++;
    }

    /* epoll_ctl */
    orig = dlsym(RTLD_NEXT, "epoll_ctl");
    if (orig) {
        sc_pipe_epoll_ctl = new ldx_sc_epoll_ctl_pipe_t(orig);
        dlreplace("epoll_ctl", (void *)wrap_epoll_ctl);
        installed_count++;
    }

    /* gethostname */
    orig = dlsym(RTLD_NEXT, "gethostname");
    if (orig) {
        sc_pipe_gethostname = new ldx_sc_gethostname_pipe_t(orig);
        dlreplace("gethostname", (void *)wrap_gethostname);
        installed_count++;
    }

    /* sethostname */
    orig = dlsym(RTLD_NEXT, "sethostname");
    if (orig) {
        sc_pipe_sethostname = new ldx_sc_sethostname_pipe_t(orig);
        dlreplace("sethostname", (void *)wrap_sethostname);
        installed_count++;
    }

    fprintf(stderr, "ldx: installed %d syscall PbV wrappers\n", installed_count);
    return installed_count;
}

extern "C" int ldx_syscall_pbv_count(void)
{
    return installed_count;
}

